{
  "buildCommand": "pnpm --filter @medishop/web build",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next",
  "env": {
    "NEXT_PUBLIC_API_URL": "https://your-render-api.onrender.com"
  }
}
